interface:
  display_name: "Paper Digest"
  short_description: "Curate and publish recent AI paper roundups"
  default_prompt: "Use $paper-digest to fetch recent AI papers, read configured interested/excluded preferences when present, curate the candidate set semantically, publish the curated digest, and notify the result."
