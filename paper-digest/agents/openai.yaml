interface:
  display_name: "Paper Digest"
  short_description: "Fetch, publish, and notify AI paper digests"
  default_prompt: "Use $paper-digest to fetch recent AI papers, format a concise digest, publish it when requested, and notify the result."
